var assert = require("assert");
var sudoku = require("../sudoku.js")

describe("first test",()=>{
  it("should work", ()=>{

    var cellList = [];
    var newCellList = sudoku.solveGame2(cellList);
    assert(true == true)

  })

});;
